//===----------------------------------------------------------------------===//
// Version string surfaced by `--version`.
//
// Committed value is a dev placeholder. The release workflow overwrites this
// file from the git tag before building, so a released binary always reports
// its tag (see .github/workflows/release.yml, "Stamp version").
//===----------------------------------------------------------------------===//

let containerComposeVersion = "0.0.0-dev"
